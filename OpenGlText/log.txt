#开始记录
#第一次记录时间为2020/8/17

#关于mvp矩阵
1.model矩阵是对象（图元）自身的变换例如放大，缩小，移动，旋转；
2.view矩阵也是lookAt矩阵，用于将所有顶点变换至摄像机视角，即view矩阵其实就是摄像机本身
3.projection矩阵（投影矩阵），在基于摄像机视角（也可以不依靠摄像机视角）的基础上，进行变换，根据公式
判断是用正交还是使用透视将所有顶点进行变换产生效果

#model矩阵，变换分解
1.变换可以分解为多个子变换，变换矩阵为互乘关系

